import 'package:flutter/material.dart';

class provinsi_user extends StatefulWidget {
  const provinsi_user({super.key});

  @override
  State<provinsi_user> createState() => _provinsi_userState();
}

class _provinsi_userState extends State<provinsi_user> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text("Hello World"),
      ),
    );
  }
}
